// Escaneos de demostración
const scans = 1240;

// Actualizar contador
document.getElementById("scanCount").textContent = scans;

// Actualizar barra
document.getElementById("progressBar").style.width =
(scans / 10000) * 100 + "%";

// Likes de demostración
document.getElementById("likes").textContent = 368;

// Escaneos de demostración
document.getElementById("scans").textContent = scans;